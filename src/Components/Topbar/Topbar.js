import React from 'react'
import "./Topbar.css"

const Topbar = () => {
  const title = "Autonomize AI - Assignment";
  return (
    <div className="top-bar">
    <div className="logo">{title}</div>
  </div>
  )
}

export default Topbar